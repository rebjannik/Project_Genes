package Default;

import java.util.Scanner;

public class Graph<T> {
	double numberOfNodes=0;
	double numberOfEdges=0;
	
	public void CreateEdge(T node1, T node2) {
		//Creates a pair - how to do this idk yet
	}
	
	public void readAndCreate() {
        Scanner sc = new Scanner(System.in);
        while (sc.hasNext()) {
            //need to create the function - when are we creating edges when are we not etc.
        }
        sc.close();
	}
	
}
